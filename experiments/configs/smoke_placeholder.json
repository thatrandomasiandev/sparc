{
  "name": "smoke_placeholder",
  "description": "Minimal config that exercises the CLI without real training.",
  "seed": 0,
  "method": "placeholder",
  "dataset": "toy",
  "epochs": 1,
  "batch_size": 8,
  "learning_rate": 1e-3,
  "metrics": ["loss"],
  "notes": "Delete or replace once the real protocol is locked in EXPERIMENTS.md."
}
