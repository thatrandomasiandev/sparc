# Experiment notebooks

Keep notebooks thin: load configs from `experiments/configs/`, call package code
from `src/research/`, and write figures to `paper/figures/` or
`experiments/artifacts/`. Prefer scripts for anything that must be reproducible.
